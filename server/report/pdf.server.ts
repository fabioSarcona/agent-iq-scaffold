export async function renderReportPdf(_data: unknown): Promise<Uint8Array> {
  // TODO: implement server-side PDF rendering (e.g., @react-pdf/renderer or Playwright printToPDF)
  return new Uint8Array();
}